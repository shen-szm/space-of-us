# Changelog

本文件记录 `Space of us` 在 V3 整理阶段的功能、修复和交付状态。

## 2026-06-13

### Added

- 新增共享列表云同步能力，覆盖地点收藏、纪念日、时光宝盒。
- 新增站点设置云同步能力，覆盖登录页文案、展示配置等全站共享设置。
- 新增 `/api/shared-items` 接口，用于统一读写共享业务数据。
- 新增 `/api/app-settings` 接口，用于统一读写全站展示设置。

### Changed

- 登录、注册、找回密码流程统一走服务端账号链路。
- 登录页、设置页、首页进度等模块改为优先读取云端数据，本地仅保留缓存与迁移用途。
- 共享数据作用域从“全站共用一份”调整为“按用户 / 情侣关系隔离”。
- `README.md` 重写为当前可交付说明，并建立更新日志约定。

### Fixed

- 修复共享数据在不同账号之间串数据的问题。
- 修复部分页面中文乱码与错误提示不可读的问题。
- 修复管理员后台在用户加载失败时无法正常进入的问题。
- 修复情侣共享配置首次迁移时没有作用域隔离的问题。

### Notes

- `space-of-us-v3` 本地分支当前领先 `origin/V3` 2 个提交。
- 当前核心链路已通过 `npm run lint` 和 `npm run build`。
- `components/CoupleHub.tsx` 仍有一部分旧中文文案需要继续清理，这是下一阶段整理重点。

## 日志规则

- 每次功能更新、修复、部署调整，都要追加到本文件。
- 仅记录真实完成并进入仓库的变更，不记录临时尝试。
