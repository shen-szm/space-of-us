# Changelog

这个文件记录 `Space of us` 在 V3 整理阶段之后的正式改动。从这一版开始，我们按“日期 + 阶段 + 结果”持续补录，后续每次更新都继续往下追加。

## 2026-06-17 · V3.4.2 音乐卡、订单闭环、反馈页与管理员后台

### Added

- 新增独立意见反馈页 `/feedback`，普通用户可从侧栏和设置页入口进入。
- 新增管理员后台页 `/admin/inbox`，包含“用户反馈收件箱”和“邮件发送中心”。
- 新增反馈持久化与接口：`POST /api/feedback`、`GET/POST /api/admin-feedback`、`POST /api/admin-mail`。
- 新增 `UserFeedback` / `FeedbackStatus` 数据结构，并补充 `ADMIN_FEEDBACK_EMAIL`、`NEXT_PUBLIC_ADMIN_USERNAME` 环境变量说明。

### Changed

- 首页音乐推荐卡重做为适配右栏窄区的 Apple Music 气质卡片，加入像素抽象封面、静态播放器和严格绑定的网易云跳转。
- 情侣订单改为接收方闭环：接收或拒绝、接收后双方待推进、接收方提交完成、发送方补一条反馈。
- 设置页改为仅保留反馈入口卡和管理员入口卡，不再把反馈表单塞回设置本体。
- 版本号同步升级到 `V3.4.2` / `v3.4.2`。

### Fixed

- 删除地图底部 `map-mist-band` 交错重复纹理，只保留低对比柔雾渐变。
- 修正音乐推荐数据结构，确保歌曲标题、艺人、封面语义和网易云链接一一对应。

## 2026-06-15 · V3.2 灵感来源、赞助支持与版本标识

### Added

- 在侧边栏“关于这个空间”卡片中新增灵感来源说明，展示原项目 GitHub 地址和抖音 ID。
- 新增赞助支持区块与赞助图片弹窗，支持点击按钮打开、点击遮罩关闭和按 `Esc` 关闭。
- 将赞助图片纳入项目静态资源，供页面弹窗直接展示。

### Changed

- 在页面左侧卡片底部新增 `V3.2` 版本标识，和 `Space of us` 品牌行一并展示。

### Fixed

- 修复 `npm run start:production` 启动后 `/_next/static/*` 资源未自动准备的问题，避免阿里云上 `/map` 等页面返回 HTML 但所需 JS/CSS chunk 加载失败。
- 新增 `npm run verify:standalone-assets` 验证脚本，用于在本地模拟生产启动并检查 `/map` 及其引用静态资源是否全部返回 `200`。

## 2026-06-15 · V3.1.2 阿里云邮件部署诊断

### Added

- 新增生产启动脚本，PM2 可通过 `npm run start:production` 启动并自动加载 `.env.production.local`。
- 新增 `npm run check:production-mail`，用于在服务器上检查 Resend 环境变量是否被运行时读取，输出不包含密钥明文。
- 新增生产环境变量加载器验证脚本，覆盖解析、保留已有进程变量和发件地址读取场景。

### Changed

- 更新阿里云交接命令，减少手动 `source .env.production.local` 后 PM2 环境变量丢失导致邮件服务未配置的风险。

### Deployment

- 阿里云服务器完成 `space-of-us.online` 与 `www.space-of-us.online` 域名绑定，Nginx 已返回 HTTPS `200 OK`。
- 使用 Certbot/Let's Encrypt 为 `space-of-us.online` 和 `www.space-of-us.online` 签发并部署 HTTPS 证书，证书自动续期由 `certbot.timer` 管理。
- Resend 发件域名 `space-of-us.online` 已验证，可将 `RESEND_FROM_EMAIL` 切换为 `Space of us <noreply@space-of-us.online>`。

### UI

- 在侧边栏“关于这个空间”卡片中新增灵感来源说明，展示原项目 GitHub 地址和抖音 ID。
- 新增赞助支持区块与赞助图片弹窗，支持点击按钮打开、点击遮罩关闭和按 `Esc` 关闭。

## 2026-06-13 · V3.1.1 认证交互修正

### Fixed

- 修复找回密码第一步发送验证码后按钮一直停在“处理中”的问题。
- 找回密码模式下隐藏底部情侣绑定模块，避免流程干扰。
- 注册时邮箱已存在的提示改为中文，直接显示“该邮箱已被使用，请换一个。”
- 注册页获取邮箱验证码后增加 60 秒倒计时，冷却结束后才允许再次发送。
- 图形验证码改为按需展示：注册页填写邮箱后自动出现，找回页填写用户名或邮箱后自动出现。
- 登录接口补齐“用户名 / 邮箱”双入口，找回密码后可直接用绑定邮箱和新密码登录。

## 2026-06-13 · V3.1 账号邮箱化与安全链路

### Added

- 新增邮箱验证码能力，支持注册验证、找回密码验证、换绑邮箱验证。
- 新增服务端图形验证码接口，用于注册和找回流程的人机校验。
- 新增密码找回授权票据，只有邮箱验证码通过后才允许提交新密码。
- 设置页新增“账号安全”区块，支持登录后主动修改密码、换绑邮箱。
- 账号公开结构补充邮箱、邮箱验证时间、最近密码更新时间。

### Changed

- 注册流程升级为“用户名 + 邮箱 + 图形验证码 + 邮箱验证码 + 密码”。
- 找回密码流程重构为两段式：先邮箱验证，再重置密码。
- 登录页移除旧的“找回口令”正式入口，不再把改密码放在登录页里。
- 管理员后台用户列表增加邮箱与验证状态展示。

### Fixed

- 清理登录页、设置页、侧边栏中的主要中文乱码。
- 统一管理员与普通用户的账号数据读取口径，继续收口到云端账号存储。

## 2026-06-13 · V3 云端同步与交付整理

### Added

- 新增共享列表云同步能力，覆盖地点收藏、纪念日、时光宝盒。
- 新增站点设置云同步能力，覆盖登录页文案、展示配置等全站共享设置。
- 新增 `/api/shared-items` 接口，用于统一读写共享业务数据。
- 新增 `/api/app-settings` 接口，用于统一读写全站展示设置。
- 新增交付文档与日志基线，开始正式记录版本整理过程。

### Changed

- 登录、注册、找回密码流程统一走服务端账号链路。
- 登录页、设置页、首页进度等模块改为优先读取云端数据，本地只保留缓存与迁移用途。
- 共享数据作用域从“全站共用一份”调整为“按用户 / 情侣关系隔离”。

### Fixed

- 修复共享数据在不同账号之间串数据的问题。
- 修复管理员后台在用户加载失败时无法正常进入的问题。
- 修复多处页面中文提示不可读的问题。

## 2026-06-12 · V3 基线整理

### Added

- 创建 `space-of-us-v3` / `origin/V3` 作为后续整理基线。
- 保留原有 Apple 风格视觉方向，开始收拢登录、后台、共享页面结构。

### Changed

- 后台继续保留用户查看、重置密码、进入主站功能。
- 共享数据与设置开始从“本地优先”转向“云端优先”。

### Fixed

- 清理一批旧部署残留与无用构建干扰项。
- 完成 V3 第一轮可运行、可构建、可部署整理。

## 2026-06-09 · 登录与后台修复阶段

### Fixed

- 修复默认认证密码兼容问题。
- 修复登录失败提示不清晰的问题。
- 修复管理员登录后因用户列表报错被整体阻塞的问题。
- 修复账号存储错误没有被正确暴露的问题。

## Logging Rule

- 每次功能更新、修复、部署调整，都要追加到本文档。
- 只记录已经真实完成并进入仓库的改动，不记录临时试验。
- 如果一次更新同时涉及“功能 + 修复 + 部署”，允许写在同一天的不同小节中。
